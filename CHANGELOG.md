# Changelog

本项目遵循 Keep a Changelog，并使用语义化版本表达发布版本。

## [Unreleased]

### Added

- 创建 Unity 2022.3 LTS 2D 项目。
- 添加需求、架构、路线图、内部接口、编码规范和验收文档。
- 添加项目级 AI Skills 与 MCP 使用说明。
- 添加 Unity Git 忽略规则和初始目录结构。
- 添加可脱离 Unity UI 测试的实验流程状态机核心。
- 添加流程状态、步骤定义、非法操作错误码和 EditMode 测试。
- 添加版本化 JSON 内容模型、结构校验和 `draft/approved` 加载门禁。
- 添加 Fe 测定占位模板；其中科学数据保持待教师审核状态。
- 添加实验会话启动编排：仅在内容校验通过后创建并启动流程状态机。
- 添加合成内容的应用层测试，覆盖已审核内容启动、草稿拦截和非法 JSON 拒绝。
- 添加线性标定计算核心，包含最小二乘拟合、R² 和结构化退化数据错误。
- 添加合成标定数据测试；不包含真实 Fe 标准溶液或仪器响应数据。
- 添加基于线性标定的样品浓度反算，并对不可用标定、零灵敏度和非法响应返回结构化错误。
- 添加合成样品响应测试；尚未实现教师审核的稀释、空白扣除或其他换算规则。
- 添加虚拟仪器供电、泵和等离子体状态机，以及操作顺序错误的结构化错误码。
- 添加仪器状态测试；默认顺序仅用于教学原型，不构成真实仪器 SOP。
- 添加本地 JSON 实验记录保存与读取，使用 GUID 文件名和临时文件替换写入。
- 添加 `SampleScene` 中的可运行 ICP-OES 原型界面，展示流程状态和仪器操作按钮。
- 添加 UI 运行验证，覆盖实验启动、错误顺序提示和开机→泵→等离子体顺序。
- 将 UI 实验启动改为读取 `StreamingAssets/content/fe-measurement.json`，保留合成 draft 内容标识。
- 将原型流程完成状态写入 `Application.persistentDataPath/records`，并在 UI 显示生成的 GUID。
- 为实验内容增加参数模型和范围校验，并在 UI 启动状态显示参数数量。
- 添加 3 个明确标记为 `SYNTHETIC_TEST_DATA` 的参数模板；不代表真实仪器参数。
