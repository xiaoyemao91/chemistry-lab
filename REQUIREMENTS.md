# 需求规格

## 1. 项目目标

构建一款可离线运行的 Windows ICP-OES 教学仿真软件，使学生能够按照受控步骤完成一次 Fe 测定实验，并理解仪器状态、分析参数、标定曲线、样品测量和结果记录之间的关系。

## 2. 用户与角色

- 学生：执行实验、接收操作提示、查看计算结果和历史记录。
- 教师/实验负责人：提供并审核 SOP、参数、公式、错误规则和参考结果。
- 开发与测试人员：将已审核内容实现为可维护、可验证的软件功能。

MVP 不提供教师后台。教师内容由开发人员转换为受版本控制的 JSON 文件。

## 3. 功能需求

| 编号 | 需求 |
| --- | --- |
| FR-001 | 软件能够在 Windows 环境离线启动。 |
| FR-002 | 学生能够创建、开始、暂停、重置并完成一次 Fe 测定实验。 |
| FR-003 | 系统按照配置控制实验步骤顺序，并阻止或提示不合法操作。 |
| FR-004 | 系统展示开机、泵、进样、等离子体和测量等教学状态。 |
| FR-005 | 学生能够输入或选择经教师确认的 ICP-OES 分析参数。 |
| FR-006 | 系统能够处理 Fe 标准溶液测量结果并建立线性标定曲线。 |
| FR-007 | 系统能够根据标定结果和教师确认的公式计算样品 Fe 浓度。 |
| FR-008 | 系统明确展示参数错误、步骤错误和无法计算的原因。 |
| FR-009 | 系统能够将实验过程、输入、结果和完成状态保存到本地。 |
| FR-010 | 系统能够再次读取并展示已保存的实验记录。 |
| FR-011 | 教学步骤、参数约束、提示、错误规则和参考数据通过 JSON 配置维护。 |
| FR-012 | 配置加载失败时，系统停止进入实验并给出可定位的错误信息。 |

## 4. MVP 用户故事

1. 作为学生，我可以按照提示完成开机检查，从而知道仪器是否具备实验条件。
2. 作为学生，我可以设置分析参数，并在参数不合理时获得明确反馈。
3. 作为学生，我可以测量一组 Fe 标准溶液并查看标定曲线和拟合指标。
4. 作为学生，我可以测量样品、计算浓度并保存完整实验记录。
5. 作为教师，我可以提供新的配置内容，而不要求开发者修改流程核心代码。

## 5. 非功能需求

### 性能

- 普通教学电脑上离线启动目标不超过 15 秒；最终指标需在目标机器验收。
- 常规页面操作应在 200 毫秒内反馈，长任务必须显示进行状态。
- 单次实验记录保存应为原子性操作，避免留下半写入文件。

### 安全与隐私

- 不在仓库中保存账号、密钥、个人信息和未授权实验室资料。
- 文件名和配置路径必须防止目录穿越。
- 配置和记录反序列化后必须执行结构与范围校验。
- 错误日志不得包含个人信息或敏感实验室数据。

### 可维护性

- 流程、计算、持久化和 UI 通过接口解耦。
- 科学规则不得散落在 MonoBehaviour 或 UI 事件中。
- 计算和规则逻辑必须可在 EditMode 中独立测试。
- 配置格式包含 `schemaVersion`，不兼容版本应明确拒绝。

### 可用性

- 页面必须显示当前步骤、下一步目标和错误恢复建议。
- 颜色不能作为唯一状态表达；同时使用文字或图标。
- 关键操作需要防止重复触发，重置操作需要明确确认。

## 6. MVP 验收标准

- 学生能够在 Windows 离线完成一条 Fe 测定闭环。
- 顺序错误和参数错误均有明确、可恢复的提示。
- 标定曲线和浓度结果与教师提供的验收数据一致。
- 实验记录能够保存、重新读取且字段完整。
- 修改合法 JSON 配置后，无需修改核心代码即可改变教学内容。
- EditMode 计算测试、PlayMode 主流程测试和教师手工验收通过。

## 7. MVP 范围外

- 生成式 AI 问答或自动生成实验内容
- 高精度三维仪器模型和流体/光谱物理仿真
- 真实仪器、传感器或串口控制
- 用户账号、联网同步、教师后台和成绩分析
- SQLite、服务端数据库、Docker、Redis、Postgres
- Fe 以外元素或其他仪器型号的完整实验

## 8. 待教师确认

仪器型号、完整 SOP、Fe 实验步骤、参数及单位、合法范围、标准溶液数据、拟合方法、样品换算公式、常见错误、界面截图和参考结果。详见 `docs/TEACHER_INPUT_CHECKLIST.md`。

