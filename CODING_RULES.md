# 开发规范

## 1. C# 与 Unity

- 使用项目 Unity 版本支持的 C# 语言特性，不通过外部编译器绕过 Unity。
- 命名空间以 `ChemistryLab` 开头，例如 `ChemistryLab.Core.Workflow`。
- 类型、公共成员和枚举使用 `PascalCase`；局部变量和参数使用 `camelCase`；私有字段使用 `_camelCase`。
- 接口以 `I` 开头；异步方法以 `Async` 结尾；布尔值使用 `Is`、`Has`、`Can` 或 `Should` 表达。
- 一个主要类型对应一个同名文件。Unity 组件文件名必须与 MonoBehaviour 类型名一致。
- MonoBehaviour 只处理 Unity 生命周期、序列化引用和 UI 绑定；科学规则和计算放入普通 C# 类型。
- 避免在 `Update` 中轮询可由事件驱动的状态，不在帧循环中分配临时集合或执行文件 IO。

## 2. 文件与资源命名

- C# 文件：`PascalCase.cs`
- 场景：`PascalCase.unity`
- Prefab：`PascalCase.prefab`
- JSON：`kebab-case.json`
- 图片与音频：语义明确的 `kebab-case`，不得使用“新建”“最终版2”等名称。
- 稳定配置 ID 使用小写 `kebab-case`，发布后不得因显示名称变化而修改。

## 3. 代码设计

- 遵循 SOLID、DRY、KISS、YAGNI；先实现当前模块的最小完整路径。
- 通过构造函数或显式初始化注入依赖，避免隐藏的全局单例。
- 优先不可变数据与纯函数；状态变化通过清晰命令或用例完成。
- 不为单个调用点创建抽象，不提前实现后端、数据库或插件扩展层。
- 参数、公式和教学提示不得硬编码在 UI 或 MonoBehaviour 中。

## 4. 注释与文档

- 注释说明“为什么”和约束，不重复代码表面含义。
- 公共接口、非显然算法和科学计算入口使用 XML 文档注释。
- 任何教师确认的公式必须记录来源、版本、单位和适用范围。
- 行为、接口或配置格式变化后，同一模块内更新对应文档和 CHANGELOG。

## 5. 错误处理与日志

- 在配置、用户输入、文件系统和外部数据边界进行校验。
- 不吞掉异常；转换为领域错误时保留可诊断原因。
- 学生提示使用可理解语言，开发日志包含稳定错误码和必要上下文。
- 日志级别：`Debug` 用于开发细节，`Info` 用于生命周期，`Warning` 用于可恢复问题，`Error` 用于失败操作。
- 日志不得记录个人信息、密钥、完整敏感路径或未经授权的实验室数据。

## 6. JSON 配置

- 文件使用 UTF-8、`camelCase` 字段和显式 `schemaVersion`。
- 加载后校验必填字段、唯一 ID、引用完整性、数值有限性、单位和范围。
- 未经教师审核的内容设置 `reviewStatus: "draft"`，不得进入发布构建。
- 不兼容配置版本必须拒绝并给出明确错误，不能静默猜测。

## 7. 测试

- 缺陷先用可重复测试或最小复现证明，再实施最小修复。
- 计算测试覆盖正常输入、无效输入、边界值、退化拟合和非有限数值。
- 流程测试覆盖合法路径、顺序错误、重复操作、暂停、重置和恢复。
- 每个功能模块完成后依次执行自测、修复、重构和文档更新，再进入下一模块。

## 8. Git

- 提交采用 Conventional Commits，例如 `feat(workflow): add experiment state machine`。
- 一次提交只包含一个清晰目的，不混入无关格式化或重构。
- 提交前检查 `git diff`、运行相关测试并确认没有敏感资料。
- 不自动创建分支、提交或推送；每一步都先取得用户明确确认。
- 禁止提交 `Library/`、`Temp/`、`Logs/`、本地 IDE 缓存和构建产物。

## 9. 安全

- 不把学生身份、负责人信息、仪器序列号、账号、密钥和未授权照片提交到仓库。
- 生成记录文件名时使用应用生成 ID，并限制到预定目录。
- 读取配置和记录时拒绝目录穿越、未知版本和超出限制的数据规模。

